crypto build on custom blockchain

### CMake build and run on macOS/Linux
- To build:




### CMake build and run on Windows
- To build:

    ```bash
    cd build
    cmake ..
    cmake --build .
    ```

- To run:

    ```bash
    .\Debug\chud_coin.exe
    ```